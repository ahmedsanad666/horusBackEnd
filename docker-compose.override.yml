services:
  mssql:
    ports:
      - "1433:1433"

  api:
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      CORS__Origins: "http://localhost:5173,http://localhost:3000,http://localhost:8080"

